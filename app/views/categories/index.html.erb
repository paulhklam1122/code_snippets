<h1>Programming Languages</h1>
<% @categories.each do |category| %>
<ul>
  <li>
    <%= link_to category.name, category_path(category) %>
    <%= "(#{category.snippets.count} Snippets)"%>
  </li>
</ul>
<% end %>
